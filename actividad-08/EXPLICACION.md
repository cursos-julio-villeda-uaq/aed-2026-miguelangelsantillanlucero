# Explicación — Actividad 8

Completa este archivo con tus propias palabras.

1. ¿Por qué `std::vector` permite trabajar con una cantidad variable de calificaciones?  Por que logra administrar la memoria dependiendo de las calificaciones que agregue el usuario
2. ¿Dónde utilizaste el `for` basado en rango y qué valor acumula? dentro de la funcion calcularSuma y va acumulando los valores en la variable suma
3. ¿Por qué no se puede calcular el promedio de un vector vacío? Por que no existe nada que evaluar, por lo tanto, el resultado es 0 y al tratar de dividir para optener el promedio dara un error por que no se puede dividir /0
4. Anota el SHA del commit final que entregaste en Classroom:
